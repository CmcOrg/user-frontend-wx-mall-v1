<view class="flex vwh100">
    <t-side-bar value="{{sideBarIndex}}" bind:change="onSideBarChange">
        <t-side-bar-item wx:for="{{categoryList}}" wx:key="index" value="{{index}}"
                         label="{{item.label}}"/>
    </t-side-bar>
    <scroll-view class="flex1" scroll-y scroll-with-animation scroll-top="{{scrollTop}}" bind:scroll="onScroll">
        <view wx:for="{{categoryList}}" wx:key="index" class="p-l-r-16">
            <view class="title p-l-10 m-t-b-4">{{item.title || item.label}}</view>
            <t-grid column="{{3}}" border="{{false}}">
                <block wx:for="{{item.itemList}}" wx:key="index" wx:for-item="subItem">
                    <t-grid-item text="{{subItem.label}}">
                        <t-image shape="round" src="{{subItem.image}}" slot="image" lazy t-class="w-48 h-48"/>
                    </t-grid-item>
                </block>
            </t-grid>
        </view>
    </scroll-view>
</view>
